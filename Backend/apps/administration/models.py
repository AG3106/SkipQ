"""
Models for the administration app (V1 — barebones).

No custom models needed for V1. Core admin entities (AdminProfile,
AdminActivityLog) reside in the users app.

Notification and FlaggedContent models are saved in
temporary/admin_removed_code_v2.txt for future versions.
"""
